{
      "name": "calculateTotal",
      "description": "Create a function that calculates the total price including tax",
      "hint": "Use the helper: `const subtotal = calculateSubtotal(items); return subtotal * (1 + taxRate);`",
      "jsTemplate": "function calculateSubtotal(items) {\n  return items.reduce((sum, item) => sum + item.price, 0);\n}\n\nfunction calculateTotal(items, taxRate) {\n  // TODO: Use the calculateSubtotal helper function and apply taxRate\n}",
      "jsTest": "    it(\"should calculate total with tax using helper function\", () => {\n      const items = [\n        { name: 'Item 1', price: 10 },\n        { name: 'Item 2', price: 20 },\n        { name: 'Item 3', price: 30 }\n      ];\n\n      assert.strictEqual(calculateTotal(items, 0.1), 66);\n      assert.strictEqual(calculateTotal(items, 0.2), 72);\n      assert.strictEqual(calculateTotal([], 0.1), 0);\n    });",
      "chapterId": "03-functions-modularCode"
    }